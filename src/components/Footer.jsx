import { StyledFooter } from "./FooterStyle"

export const Footer = () => {
    return(
        <StyledFooter>
            <h2>Desenvolvido por <span>Ryan Silva</span></h2>
        </StyledFooter>
    )
}